import {
    Card, CardImg, CardTitle, CardSubtitle
} from 'reactstrap';

const ChildComponent = (props) => {
    return (
        <div>
            Child Component
        </div>
    )
}

export default ChildComponent;